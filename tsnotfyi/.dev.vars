# Local dev overrides — wrangler dev reads these instead of [vars] in wrangler.toml
# Use mini.local so browser can reach them (localhost won't work from other machines)
AUDIO_SERVER_URL=http://mini.local:3002
API_SERVER_URL=http://mini.local:3003
COVERS_URL=http://mini.local:3004
